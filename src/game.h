#ifndef _GAME_H
#define _GAME_H

#include <vector>

#include "bloc.h"

class Game {
  // std::vector<std::vector<Wall, Floor, Box, Target>> matrix;
public:
  void show_menu();
  Game();
  // ~Game();
};

#endif